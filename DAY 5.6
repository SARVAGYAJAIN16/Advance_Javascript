Understand about object (some useful method ) in javascript...

// object useful method in javascript......
const product = {
       id: 1,
       name: "Laptop",
       category: "Computers",
       brand: "ExampleBrand",
       price: 999.99,
       stock: 50,
       description:
         "Powerful laptop with a quad-core i5 processor, 8GB RAM, 256GB SSD, and a 14-inch FHD display.",
      image: "image is not found",
     };

    // 1  Object.keys():
    let keys = Object.keys(product);
    console.log(keys);
// output [
  //'id',
 // 'name',
 // 'category',
 // 'brand',
 // 'price',
 // 'stock',
 // 'description',
 // 'image'
// ]

// 2 Object.value():
let keys2 = Object.values(product);
console.log(keys2);
// output   ...we will get all keys 

// 3 Object.entries():
let keys3 = Object.entries(product);
console.log(keys3);
// output we get in key : value pair..and convert in array then it return

// 4 Object.hasOwnProperty():
console.log(product.hasOwnProperty("name")); // true
console.log(product.hasOwnProperty("isStudent")); // false

// 5 Object.assign():
const target = {a:1, b:2};
const source = {b:3, c:4};
//const mergedObject = Object.assign({}, target , source);// output { a: 1, b: 3, c: 4 }
// or 
const mergedObject = Object.assign(target , source); // { a: 1, b: 3, c: 4 }
console.log(mergedObject);
// to safe from call by value we use Object.assign
// empty curly braces is a target
// source will copy in target.

// 6 Object.freeze(): use for single time you created Object then with that object dont do 
// anything.

Object.freeze(product);
product.id ="5735";
console.log(product); // id will not get change..
